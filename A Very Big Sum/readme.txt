Start.

Initialize a variable sum ← 0.

For each index i from 0 to length(ar) - 1:

Check if ar[i] is a number.

If true, add ar[i] to sum.

If false, skip this element.

After completing the loop, return the value of sum.

End.