Calculate distance from Cat A to the mouse:

distanceCatA = absolute value of (z - x)

Calculate distance from Cat B to the mouse:

distanceCatB = absolute value of (z - y)

Compare distances:
a. If distanceCatA < distanceCatB:

Return 'Cat A' → Cat A reaches the mouse first
b. Else if distanceCatB < distanceCatA:

Return 'Cat B' → Cat B reaches the mouse first
c. Else:

Return