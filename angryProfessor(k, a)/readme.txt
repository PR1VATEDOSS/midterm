Count on-time students:

onTimeCount = number of students in array a with arrival time ≤ 0

Compare with threshold k:

If onTimeCount < k:

Return "YES" → Class is canceled

Else:

Return "NO" → Class is held