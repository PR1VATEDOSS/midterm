Find the tallest hurdle:

maxHeight = maximum value in height array

Calculate extra doses needed:

extraDoses = maxHeight - k

Ensure non-negative result:

If extraDoses < 0, set extraDoses = 0

Return