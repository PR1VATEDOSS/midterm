Start.

Find the maximum height among all elements in candles and store it in max.

Initialize count ← 0.

For each index i from 0 to length(candles) - 1:

If candles[i] is equal to max, then increment count.

After the loop ends, return count.

End.